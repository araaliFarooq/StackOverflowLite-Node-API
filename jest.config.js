module.exports = {
  roots: ['<rootDir>/src'],
  testEnvironment: 'node'
  // preset: 'ts-jest', // use this if you are using TypeScript
  // globalSetup: './src/config/jest.global-setup.js', // optional: will be called once before all tests are executed
  // globalTeardown: './src/config/jest.global-teardown.js' // optional: will be called once after all tests are executed
};
